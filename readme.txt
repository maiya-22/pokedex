Site in progress:

Steps to view:

1) Click "fetch trainer Chuck's pokemon" button.
2) Type "dragonair" "charmeleon" or "butterfree" in the search bar and click the "go" button.
3) Uses CSS grid, so best view is in Chrome browser.

Background animation rendering when loaded from desktop, but not seeming to work properly from github.io    s